# التثبيت على أي خادم Windows

الحزمة غير مرتبطة بعنوان IP أو باسم جهاز محدد.

1. فك ضغط الحزمة على الخادم المطلوب.
2. شغّل `INSTALL-WIZARD.cmd` كمسؤول.
3. اترك حقل SQL Server على القيمة المكتشفة تلقائيًا إذا كانت قاعدة البيانات على الخادم نفسه، أو أدخل اسم خادم SQL الفعلي إذا كانت منفصلة.
4. اختر منفذ التطبيق، والقيمة الافتراضية هي `8000`.
5. بعد اكتمال التثبيت يعرض المعالج رابط الجهاز المحلي ورابط الشبكة المكتشف تلقائيًا.

يعمل التطبيق على جميع واجهات الشبكة باستخدام `0.0.0.0`، وتستخدم الواجهة مسارات API نسبية؛ لذلك لا تحتوي ملفات النظام على عنوان IP ثابت. إذا تغير عنوان الخادم لاحقًا، استخدم العنوان الجديد مع منفذ التطبيق دون تعديل البرنامج.

يمكن تشغيل `VERIFY-SERVER.cmd` للفحص. يبدأ تلقائيًا باسم الجهاز الحالي، ويمكن تغييره يدويًا إلى اسم جهاز أو عنوان IP آخر.
